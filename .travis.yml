language: cpp

before_install: 
 - sudo apt-get update
 - sudo apt-get install libconfig++-dev libgtest-dev
 - "cd /usr/src/gtest && sudo cmake . && sudo cmake --build . && sudo mv libg* /usr/local/lib/ ; cd -"
before_script:
 - cmake . -Dbuild_tests=BOOL:ON && ./test-all

script: make
